import SelectableAbilityCard from "@/components/ability-cards/selectable-ability-card";

describe("SelectableAbilityCard", () => {
    it("renders a button", () => {});

    it("renders the card", () => {});
});

describe("toggleAbilityCardUnlock", () => {
    it("unlocks a card", () => {});

    it("locks a card that is already unlocked", () => {});
});
